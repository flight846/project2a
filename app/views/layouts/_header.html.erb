<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">Query.me</a>
      </div>
      <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-right">
            <% if user_logged_in? %>
                <li><%= link_to current_user.email, user_path(current_user) %></li>
                <li><%= link_to 'Submit a Query', new_user_post_path(current_user) %></li>
                <li role="separator" class="divider"></li>
                <li><%= link_to "Sign Out", sign_out_path, method: :delete %></li>
            <% else %>
                <% if !(current_page?(sign_in_path)) %>
                    <li> <%= link_to "Sign In", sign_in_path %></li>
                <% end %>
                <% if !(current_page?(signup_path)) %>
                    <li><%= link_to "Sign Up", signup_path %></li>
                <% end %>
            <% end %>
          </ul>
      </div>
    </div>
  </nav>